package sorny.domain.prediction;

/**
 * Enum for prediction came true status
 * HELLYEAH = Yes, it came true
 * PARTIALLY = Some of it came true
 * NAH = Didn't come true
 * SECRET = Prediction hasn't been revealed yet
 */
public enum CameTrueStatus {
    HELLYEAH, PARTIALLY, NAH, SECRET
}
